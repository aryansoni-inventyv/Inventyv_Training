import { FilterTaskPipePipe } from './filter-task-pipe-pipe';

describe('FilterTaskPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTaskPipePipe();
    expect(pipe).toBeTruthy();
  });
});
