import { XyzDirective } from './xyz-directive';

describe('XyzDirective', () => {
  it('should create an instance', () => {
    const directive = new XyzDirective();
    expect(directive).toBeTruthy();
  });
});
